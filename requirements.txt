# Single source of truth for backend Python dependencies.
# Install everything with (from the repository root): uv pip install -r requirements.txt

# --- Core (always required: data pipeline + machine learning) ---
numpy>=2.0,<3
openpyxl>=3.1,<4
pyxlsb>=1.0.10,<2
fastapi>=0.139,<1
uvicorn[standard]>=0.51,<1

# --- Dev tooling (tests, lint) ---
pytest>=8.4,<9
ruff>=0.12,<1

# --- Fashion matching (visual retrieval: encoders, image processing) ---
ftfy>=6.3,<7
open-clip-torch>=3.2,<4
Pillow>=11,<12
faiss-cpu>=1.8,<2; platform_system != "Darwin"
torch>=2.7,<3
transformers>=4.52,<5
